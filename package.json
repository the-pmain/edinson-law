{
  "name": "edison-law",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "predev": "node scripts/render.mjs",
    "dev": "vite",
    "prebuild": "node scripts/render.mjs",
    "build": "vite build",
    "start": "node server.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "render": "node scripts/render.mjs"
  },
  "dependencies": {
    "vite": "^8.2.2"
  }
}
